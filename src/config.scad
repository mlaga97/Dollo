include <old/globals.scad>;
include <old/include.scad>;

/*** Frame Configuration ***/
    
    // Base Config
    unit_length = 30;
    
    // Dovetails (To Be Fixed)
    max_width=10;
    min_width=5;
    depth=5;
    height=unit_length/2;
